#!/bin/bash
# This file was auto-generated by generate_loader
# It sources all .sh scripts in its own directory and subdirectories.

# Guard to prevent sourcing multiple times
if [[ -n "$LOADER_DEEPSHIFT_87EDB077_LOADED" ]]; then
  return 0
fi
export LOADER_DEEPSHIFT_87EDB077_LOADED=1

# Determine this script's own directory and export it.
export LOADER_DEEPSHIFT_87EDB077_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd)"

# Source all scripts relative to our exported path
source "$LOADER_DEEPSHIFT_87EDB077_DIR/codeShift.sh"
source "$LOADER_DEEPSHIFT_87EDB077_DIR/deepShift.sh"
source "$LOADER_DEEPSHIFT_87EDB077_DIR/dirShift.sh"
source "$LOADER_DEEPSHIFT_87EDB077_DIR/utils/deepShiftIsPathIgnored.sh"
